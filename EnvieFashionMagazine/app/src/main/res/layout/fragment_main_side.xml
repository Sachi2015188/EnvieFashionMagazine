<?xml version="1.0" encoding="utf-8"?>
<!-- TODO srcに直接画像をいれない -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="@color/black"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum=".5">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight=".2">
        <TextView
            android:id="@+id/fragment_main_side_description"
            android:textColor="@color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:textAppearance="?android:attr/textAppearanceLarge"
            />
        </RelativeLayout>

    <ImageView
        android:id="@+id/fragment_main_side_download"
        android:background="@drawable/push_button"
        android:src="@drawable/ic_android_download"
        android:clickable="true"
        android:layout_marginBottom="@dimen/ui_diff_small"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:tint="@color/white"
        android:layout_weight=".1"/>

    <ImageView
        android:id="@+id/fragment_main_side_openDrawer"
        android:background="@drawable/push_button"
        android:src="@drawable/ic_android_more"
        android:clickable="true"
        android:layout_marginBottom="@dimen/ui_diff_small"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:tint="@color/white"
        android:layout_weight=".1"/>

    <ImageView
        android:id="@+id/fragment_main_side_library"
        android:background="@drawable/push_button"
        android:src="@drawable/ic_android_book"
        android:clickable="true"
        android:layout_width="match_parent"
        android:tint="@color/white"
        android:layout_height="0dp"
        android:layout_weight=".1"/>

</LinearLayout>